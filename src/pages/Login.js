import React from "react";
import LoginForm from "../components/LoginForm";

const Login = () => {
    return (
        <div>
            <h1>Welcome to Fetch Dog Adoption</h1>
            <LoginForm />
        </div>
    );
};

export default Login;
